import { PatternDetail } from "@/components/showcase/pattern-detail"
import { APP_SHELL_SAMPLES } from "@/components/patterns/app-shell-samples"

export default function AppShellPatternsPage() {
  return <PatternDetail slug="app-shell" samples={APP_SHELL_SAMPLES} />
}
